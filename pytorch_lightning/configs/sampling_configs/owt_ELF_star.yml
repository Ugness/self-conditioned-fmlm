# CDEQ Stage-2 sampler sweep.
- sampling_method: cdeq
  num_sampling_steps: [8, 16, 32]
  cfgs: [1.0]                       # unconditional OWT
  self_cond_cfg_scales: [3.0]  # w sweep
  time_schedule: logit_normal       # P_mean/P_std from cfg.denoiser_p_*
  sde_gamma: 1.5
